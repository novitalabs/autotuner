# Worker Configuration Template
# Copy this file to .env.worker on remote worker machines

# Worker Identity (optional - auto-generated if not set)
# WORKER_ID=rtx4090-worker

# Worker Alias (friendly name shown in dashboard)
# Use quotes for values with spaces
# WORKER_ALIAS="RTX4090 Server"

# Redis Connection (point to manager's Redis)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Deployment Mode
# Options: docker, local, ome
DEPLOYMENT_MODE=docker

# Model Path (for Docker mode)
DOCKER_MODEL_PATH=/mnt/data/models

# Worker Capacity
# WORKER_MAX_PARALLEL=2

# HuggingFace Token (for gated models)
HF_TOKEN=

# Network Proxy (if needed for HuggingFace downloads)
# HTTP_PROXY=http://proxy:port
# HTTPS_PROXY=http://proxy:port
NO_PROXY=localhost,127.0.0.1
